import React from "react";
import Dropzone from "react-dropzone";
import axios from "./axios";

function fileUpload({
  file,
  previewSrc,
  isPreviewAvailable,
  dropRef,
  setFile,
  setPreviewSrc,
  setIsPreviewAvailable,
}) {
  return <div className="upload-section"></div>;
}

export default fileUpload;
